import React from 'react';
import Header from '@/components/Header';

const Dashboard = () => {
  return (
    <div>
      <Header />
      <h1 className="text-2xl font-bold p-4">Dashboard</h1>
      {/* We will compose components here */}
    </div>
  );
};

export default Dashboard; 